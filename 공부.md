# 🎓 부경대 주변 맛집 크롤링 프로젝트 - 전체 학습 가이드

> 이 문서는 네이버 API를 활용하여 부경대 주변 음식점 데이터를 수집하고, 정제하고, GitHub에 동기화하는 **전체 과정**을 공부용으로 정리한 것입니다.
> 
> **🔰 비전공자도 이해할 수 있도록** 모든 기술 용어에 일상 비유를 달아두었습니다. 처음 보는 단어가 나오면 맨 아래의 [용어 사전](#18-용어-사전-비전공자를-위한-모든-용어-정리)을 참고하세요!

---

## 🚀 이 문서를 읽기 전에: "도대체 이 프로젝트가 뭐 하는 거야?"

**한 줄 요약**: 네이버 지도에서 "부경대 근처 맛집"을 자동으로 검색해서, 엑셀 파일(CSV)로 깔끔하게 정리하는 프로그램을 만든 것입니다.

### 🍜 비유로 이해하기

이 프로젝트를 **음식 배달 앱을 만드는 첫 번째 단계**라고 생각해 보세요.

배달 앱을 만들려면 먼저 "우리 학교 근처에 어떤 음식점이 있는지" 데이터가 필요합니다. 직접 돌아다니며 조사할 수도 있지만, 그건 너무 오래 걸리죠? 그래서 **네이버 지도가 이미 알고 있는 정보를 자동으로 가져오는 프로그램(크롤러)**을 만든 겁니다.

```
일반인이 하는 방법:
  네이버 지도 앱 열기 → "부경대 한식" 검색 → 결과 하나씩 보면서 메모장에 적기
  → "부경대 일식" 검색 → 또 적기 → "부경대 중식" 검색 → 또 적기 → ... (수백 번 반복)
  → 3일 걸림 😭

우리가 한 방법:
  파이썬 프로그램이 자동으로 2,772번 검색 → 결과를 엑셀 파일에 자동 저장
  → 2시간 만에 완료 🎉
```

### 📚 사전 지식 체크리스트

아래 내용을 전혀 몰라도 이 문서를 읽을 수 있습니다! 모르는 것은 이 문서에서 다 설명해 줍니다.

| 알면 좋지만 몰라도 OK | 이 문서에서 배울 수 있는 것 |
|---------------------|------------------------|
| 파이썬(Python) 기초 | 변수, 반복문, 조건문의 기본 구조 |
| API가 뭔지 | "프로그램끼리 대화하는 방법"이라고 생각하면 됨 |
| Git/GitHub | "코드 저장소"라고 생각하면 됨 |
| JSON, CSV | "데이터를 저장하는 파일 형식"이라고 생각하면 됨 |
| 터미널/명령어 | "마우스 대신 키보드로 컴퓨터에게 명령하는 창" |

---

## 📋 목차

**[Part 1: 기본 개념]**
1. [프로젝트 개요](#1-프로젝트-개요)
2. [환경 설정 (Step 1)](#2-환경-설정-step-1)
3. [네이버 API 이해하기 (Step 2)](#3-네이버-api-이해하기-step-2)

**[Part 2: 크롤러 만들기]**
4. [크롤러 핵심 로직 분석 (Step 3)](#4-크롤러-핵심-로직-분석-step-3)
5. [429 에러(Rate Limit) 해결 (Step 4)](#5-429-에러rate-limit-해결-step-4)
6. [백그라운드 자동화 실행 (Step 5)](#6-백그라운드-자동화-실행-step-5)

**[Part 3: 데이터 정제]**
7. [데이터 병합 및 CSV 변환 (Step 6)](#7-데이터-병합-및-csv-변환-step-6)
8. [카테고리 재분류 작업 (Step 7)](#8-카테고리-재분류-작업-step-7)

**[Part 4: 배포 및 관리]**
9. [GitHub 동기화 (Step 8)](#9-github-동기화-step-8)

**[Part 5: 정리 및 복습]**
10. [사용된 핵심 기술 정리](#10-사용된-핵심-기술-정리)
11. [파일 구조 총정리](#11-파일-구조-총정리)

**[Part 6: 심화 학습]**
12. [Windows 환경 트러블슈팅](#12-windows-환경-트러블슈팅)
13. [Python 핵심 개념 딥다이브](#13-python-핵심-개념-딥다이브)
14. [전체 데이터 흐름 아키텍처](#14-전체-데이터-흐름-아키텍처)
15. [utils.py 코드 완전 해부](#15-utilspy-코드-완전-해부)
16. [실제 디버깅 사례 모음](#16-실제-디버깅-사례-모음)
17. [처음부터 재현하기 (From Scratch Tutorial)](#17-처음부터-재현하기-from-scratch-tutorial)

**[부록]**
18. [용어 사전: 비전공자를 위한 모든 용어 정리](#18-용어-사전-비전공자를-위한-모든-용어-정리)

---

## 1. 프로젝트 개요

### 🎯 목표
부경대학교 주변 **반경 1km 이내**의 진짜 음식점 데이터를 수집하여, 깔끔하게 카테고리별로 분류된 CSV 파일(=엑셀 파일)을 만드는 것.

> 🔰 **비전공자 팁**: "위도 35.13, 경도 129.10"은 부경대의 GPS 좌표입니다. 구글 지도에서 아무 곳이나 길게 누르면 그 위치의 좌표가 나옵니다. 이걸 프로그램에 알려줘야 "여기서 1km 이내"를 계산할 수 있습니다.

### 📊 최종 결과
- **총 767개** 음식점 데이터 수집 완료
- **12개 카테고리**로 100% 분류 완료 (한식, 일식, 중식, 양식, 카페, 분식, 치킨, 피자, 햄버거, 고기, 해산물, 맛집)
- `merged_restaurants_result.csv` 파일로 최종 출력 (이 파일을 엑셀로 열면 표 형태로 볼 수 있습니다!)

---

## 2. 환경 설정 (Step 1)

> 🔰 **비전공자 비유**: 이 단계는 요리를 시작하기 전에 **재료를 준비하는 것**과 같습니다. 냄비(파이썬), 양념(라이브러리), 레시피(코드), 그리고 마트 회원카드(API 키)를 먼저 갖춰야 요리(크롤링)를 시작할 수 있습니다.

### 2-1. 필요한 API 키 발급

네이버 클라우드 플랫폼(NCP)에서 API 키를 발급받아야 합니다.

> 🔰 **API 키가 뭔가요?**: 네이버가 "너 누구야? 정말 개발자 맞아?"라고 확인하는 **출입증**입니다. 아무나 네이버 데이터를 마구 가져가면 안 되니까, 먼저 네이버 클라우드 플랫폼(cloud.naver.com)에 가입하고 "저 API 쓸게요!"라고 신청하면 ID와 비밀번호(Secret)를 발급해 줍니다. 이걸 코드에 넣어야 네이버가 우리 요청을 받아줍니다.

```
필요한 키:
- NAVER_CLIENT_ID     : NCP API Hub 클라이언트 ID
- NAVER_CLIENT_SECRET : NCP API Hub 클라이언트 시크릿
- NCP_CLIENT_ID       : 네이버 클라우드 지도 API 클라이언트 ID (웹 지도용)
```

### 2-2. `.env` 파일 생성

프로젝트 루트 디렉토리(=프로젝트 폴더의 맨 위)에 `.env` 파일을 만들어서 API 키를 안전하게 보관합니다.

> 🔰 **왜 `.env` 파일을 따로 만드나요?**: API 키는 **내 집 열쇠**와 같습니다. 코드에 직접 쓰면 GitHub에 올렸을 때 전 세계 사람이 내 열쇠를 볼 수 있게 됩니다! 그래서 `.env`라는 별도 금고에 넣어두고, `.gitignore`에 등록하여 GitHub에 절대 올라가지 않게 막는 겁니다.

```env
# .env 파일 예시 (절대 GitHub에 올리면 안 됨!)
NAVER_CLIENT_ID=발급받은_클라이언트_ID
NAVER_CLIENT_SECRET=발급받은_클라이언트_SECRET
NCP_CLIENT_ID=네이버클라우드_클라이언트_ID
```

### 2-3. 파이썬 패키지 설치

```bash
pip install requests pandas python-dotenv urllib3
```

### 💡 여기서 배울 점
- **`.env` 파일**: API 키 같은 민감한 정보를 코드에 직접 쓰지 않고 환경변수로 분리하는 보안 패턴
- **`python-dotenv`**: `.env` 파일의 내용을 자동으로 환경변수로 로드해주는 라이브러리
- **`.gitignore`**: `.env` 파일이 GitHub에 올라가지 않도록 반드시 `.gitignore`에 추가해야 함

---

## 3. 네이버 API 이해하기 (Step 2)

> 🔰 **비전공자 비유**: API는 **식당 주문 시스템**과 같습니다.
> - 메뉴판 = API 문서 ("이런 것들을 요청할 수 있어")
> - 주문서 = HTTP 요청 ("'대연동 한식' 검색해줘!")
> - 음식 = HTTP 응답 ("여기 결과 5개야!")
> - 주문 번호 = API 키 ("주문한 사람이 누구인지 확인")
> 
> 즉, 우리 프로그램이 네이버 서버에게 "대연동 한식 검색해줘!"라고 요청하면, 네이버가 결과를 JSON이라는 형식으로 돌려주는 것입니다.

### 3-1. 사용한 API 종류

우리가 사용한 네이버 API는 총 3가지입니다:

| API 이름 | 용도 | 엔드포인트 |
|---------|------|-----------|
| **Local Search** | 식당 이름, 주소, 좌표, 카테고리 검색 | `/search/v1/local` |
| **Blog Search** | 블로그 리뷰 수집 (나중에 제외함) | `/search/v1/blog` |
| **Image Search** | 식당 대표 이미지 수집 | `/search/v1/image` |

### 3-2. API 호출 방법

```python
import requests

headers = {
    "X-NCP-APIGW-API-KEY-ID": "내_클라이언트_ID",
    "X-NCP-APIGW-API-KEY": "내_클라이언트_SECRET",
    "Host": "naverapihub.apigw.ntruss.com"      # 필수!
}

params = {
    "query": "대연동 한식",   # 검색어
    "display": 5,             # 한 번에 가져올 결과 수 (최대 5개)
    "start": 1,               # 검색 시작 위치
    "sort": "random",         # 정렬 방식 (random = 정확도순)
}

response = requests.get(
    "https://naverapihub.apigw.ntruss.com/search/v1/local",
    headers=headers,
    params=params,
    timeout=(5.0, 15.0)
)

data = response.json()
items = data.get("items", [])   # 검색 결과 리스트
```

### 3-3. API 응답 구조

네이버 Local Search API가 돌려주는 각 아이템(item)의 구조:

```json
{
    "title": "<b>홍콩반점</b> 경성대점",    // HTML 태그가 포함된 상호명
    "link": "https://...",                   // 네이버 링크
    "category": "음식점>중국식>중국요리",     // 네이버가 분류한 카테고리
    "description": "",                       // 설명
    "telephone": "051-621-0410",             // 전화번호
    "address": "부산 남구 대연동 55-1",       // 지번 주소
    "roadAddress": "부산 남구 수영로 316",    // 도로명 주소
    "mapx": "1290985",                       // X좌표 (카텍 좌표계)
    "mapy": "351385"                         // Y좌표 (카텍 좌표계)
}
```

### 💡 여기서 배울 점
- **REST API**: HTTP GET 요청으로 데이터를 요청하고, JSON 형태로 응답을 받는 표준 방식
- **Headers**: API 인증 정보를 HTTP 헤더에 담아 보내는 방식
- **Query Parameters**: `?query=대연동+한식&display=5` 처럼 URL 뒤에 붙여 보내는 검색 조건
- **카텍 좌표계 vs WGS84**: 네이버 API는 카텍(KATEC) 좌표를 반환하지만, 실제 지도에서 쓰려면 WGS84(위도/경도)로 변환해야 함

---

## 4. 크롤러 핵심 로직 분석 (Step 3)

### 4-1. 검색 전략: "행정동 × 카테고리" 조합

단순히 "부경대 맛집"이라고만 검색하면 네이버 API가 최대 5개밖에 안 돌려줍니다.
그래서 우리는 **행정동 14개 × 카테고리 198개 = 총 2,772개의 검색 조합**을 만들어서 빈틈없이 검색했습니다.

```python
# config.py에서 정의한 검색 지역 (행정동 14개)
SEARCH_AREA_KEYWORDS = [
    "대연1동", "대연2동", "대연3동", "대연4동", "대연5동", "대연6동",
    "문현1동", "문현2동", "문현3동", "문현4동",
    "용호1동", "용호2동", "용호3동", "용호4동"
]

# config.py에서 정의한 카테고리 매핑 (11개 대분류, 198개 세부 키워드)
CATEGORY_MAPPING = {
    "한식": ["한식", "국밥", "해장국", "돼지국밥", ...],  # 43개
    "일식": ["일식", "초밥", "스시", "돈까스", ...],      # 24개
    "중식": ["중식", "짜장면", "짬뽕", "마라탕", ...],    # 18개
    # ... 총 198개 세부 키워드
}
```

검색 루프의 핵심:
```python
for area in areas:                    # 14개 행정동
    for category in categories:       # 198개 카테고리
        query = f"{area} {category}"  # 예: "대연3동 돼지국밥"
        items = search_naver_local(query)
        # ... 결과 처리
```

### 4-2. 3단계 필터링 파이프라인

네이버 API가 돌려주는 결과에는 쓰레기 데이터가 매우 많습니다.
(예: "부산 화장품 가게", "용호동 복권 판매점" 등)

우리는 3단계 필터링으로 이를 완벽하게 걸러냈습니다:

```
[네이버 API 응답] 
    → ① 카테고리 필터: "음식점", "카페", "간식" 등이 포함된 것만 통과
    → ② 중복 필터: 이미 수집한 식당 이름(정규화)이면 버림
    → ③ 거리 필터: 부경대에서 1km 이내가 아니면 버림
    → [최종 저장]
```

```python
# ① 카테고리 필터 (쓰레기 데이터 원천 차단)
api_category = item.get("category", "")
valid_categories = ["음식점", "카페", "주점", "간식", "베이커리", "패스트푸드", "뷔페"]
if not any(valid in api_category for valid in valid_categories):
    continue  # 화장품, 복권 등 무관한 업종은 즉시 버림

# ② 중복 체크 (이름 정규화 후 비교)
name_norm = normalize_name(raw_name)  # 공백, 특수문자 제거
if name_norm in seen_names:
    continue  # 이미 수집한 식당이면 건너뜀
seen_names.add(name_norm)

# ③ 거리 필터 (반경 1km 이내만)
if not is_within_radius(lat, lng, SCHOOL_LAT, SCHOOL_LNG, 1.0):
    return None  # 1km 밖이면 즉시 폐기
```

### 4-3. 좌표 변환과 거리 계산

```python
# 카텍 좌표 → WGS84 변환 (utils.py에 구현)
def katec_to_wgs84(mapx, mapy):
    """네이버 API의 카텍 좌표를 위도/경도로 변환"""
    # ... 변환 로직
    return lat, lng

# 두 지점 간 직선 거리 계산 (Haversine 공식)
def haversine_distance(lat1, lng1, lat2, lng2):
    """지구 표면 위 두 점 사이의 직선 거리를 km 단위로 계산"""
    R = 6371  # 지구 반지름 (km)
    # ... 삼각함수를 이용한 계산
    return distance_km
```

### 4-4. 청크(Chunk) 저장 방식

데이터를 50개씩 모아서 파일로 저장합니다. 이렇게 하면:
- 크롤링 도중 프로그램이 죽어도 이미 저장된 데이터는 안전함
- 이어서 크롤링할 때 기존 청크를 읽어서 중복 체크 가능

```python
CHUNK_SIZE = 50

# 50개가 모이면 저장
if len(current_chunk_data) >= CHUNK_SIZE:
    save_chunk(current_chunk_data, chunk_index)  # restaurants_chunk_3.json 등
    chunk_index += 1
    current_chunk_data = []  # 버퍼 비우기
```

### 💡 여기서 배울 점
- **데이터 파이프라인**: 원본 데이터 → 필터링 → 변환 → 저장의 흐름
- **정규화(Normalization)**: 비교를 위해 데이터를 통일된 형태로 변환하는 것 (공백 제거, 소문자 변환 등)
- **Haversine 공식**: GPS 좌표 두 개로 지구 표면 위의 실제 거리를 구하는 수학 공식
- **청크 저장**: 대량 데이터를 안전하게 나눠서 저장하는 패턴

---

## 5. 429 에러(Rate Limit) 해결 (Step 4)

### 5-1. 문제 상황

네이버 API를 너무 빠르게 연속으로 호출하면 **HTTP 429 (Too Many Requests)** 에러가 발생합니다.
이것은 네이버가 "너 API를 너무 많이 쓰니까 잠깐 쉬어!" 라고 차단하는 것입니다.

### 5-2. 해결 방법: 자동 재시도(Retry) + 백오프(Backoff)

```python
from requests.adapters import HTTPAdapter
from urllib3.util.retry import Retry

# Session 객체 생성 (연결을 재사용하여 효율적)
api_session = requests.Session()

# 재시도 전략 설정
retry_strategy = Retry(
    total=10,                    # 최대 10번까지 재시도
    backoff_factor=3.0,          # 대기 시간을 3배씩 늘림 (3초, 6초, 12초, 24초...)
    status_forcelist=[429, 500, 502, 503, 504],  # 이 에러코드들에 대해 자동 재시도
)

# HTTPAdapter에 재시도 전략 장착
adapter = HTTPAdapter(max_retries=retry_strategy)
api_session.mount("https://", adapter)
api_session.mount("http://", adapter)
```

### 5-3. DNS 병목 해결 (고급 기법)

API를 수천 번 호출하다 보면, 매번 DNS(도메인 → IP 변환)를 조회하는 것도 병목이 됩니다.
우리는 네이버 API 서버의 IP를 미리 알아내서 직접 접속했습니다.

```python
# config.py
# 일반적인 방법 (DNS 조회 필요):
# NAVER_LOCAL_SEARCH_URL = "https://naverapihub.apigw.ntruss.com/search/v1/local"

# 최적화된 방법 (DNS 건너뜀):
NAVER_LOCAL_SEARCH_URL = "https://110.234.154.13/search/v1/local"

# 대신 Host 헤더를 수동으로 설정해줘야 함
headers = {
    "Host": "naverapihub.apigw.ntruss.com",  # 서버가 어떤 도메인인지 알려줌
    # ...
}
```

### 5-4. API 호출 최소화 전략

```
[최적화 전]
검색 → 결과 5개 받음 → 5개 모두에 대해 블로그/이미지 API 추가 호출
= 검색 1번당 API 호출 11번 (1 + 5×2)

[최적화 후]  
검색 → 결과 5개 받음 → 좌표로 거리 먼저 계산 → 1km 밖이면 즉시 버림
= 거리 밖인 식당에 대해서는 추가 API 호출 0번!
```

이 최적화로 **불필요한 API 호출을 약 70% 이상 줄였습니다.**

### 💡 여기서 배울 점
- **Rate Limiting**: 서버가 클라이언트의 요청 빈도를 제한하는 보호 메커니즘
- **Exponential Backoff**: 재시도 간격을 지수적으로 늘리는 전략 (3초 → 6초 → 12초 → ...)
- **Session 재사용**: TCP 연결을 재사용하여 매번 새 연결을 맺는 오버헤드를 줄이는 기법
- **DNS 최적화**: 도메인 대신 IP를 직접 사용하여 DNS 조회 시간을 절약하는 기법

---

## 6. 백그라운드 자동화 실행 (Step 5)

### 6-1. 크롤러를 백그라운드에서 실행

총 2,772개의 검색 조합을 돌려야 하므로, 시간이 매우 오래 걸립니다.
그래서 프로그램을 백그라운드에서 실행하고, 로그 파일로 진행 상황을 확인합니다.

```bash
# 백그라운드 실행 + 로그 파일에 출력 저장
python -u collector.py >> collector_v14_resume.log 2>&1
```

각 옵션의 의미:
- `python -u`: 파이썬의 출력 버퍼링을 끔 (실시간으로 로그에 기록)
- `>>`: 기존 로그 파일에 이어서 쓰기 (> 는 덮어쓰기)
- `2>&1`: 에러 출력(stderr)도 같은 파일에 저장

### 6-2. 실시간 모니터링

```bash
# 로그 파일의 마지막 15줄 확인 (진행 상황 확인)
Get-Content collector_v14_resume.log -Tail 15

# 또는 Linux/Mac에서는:
tail -f collector_v14_resume.log
```

### 6-3. 자동 헬스체크 (1분/30분 주기)

크롤러가 멈추거나 에러가 나면 자동으로 감지하기 위해, 별도의 모니터링을 설정했습니다:

- **1분마다**: 로그 파일의 마지막 15줄을 확인하여 에러(429, crash 등) 감시
- **30분마다**: 전체 진행 상황 보고 (몇 번째 쿼리까지 완료했는지, 데이터 몇 개 수집했는지)

### 💡 여기서 배울 점
- **백그라운드 프로세스**: 터미널을 닫아도 프로그램이 계속 실행되게 하는 방법
- **로그 리다이렉션**: `>>` 와 `2>&1` 를 사용한 출력 저장
- **`-u` 플래그**: 파이썬의 출력 버퍼링 비활성화 (디버깅 시 필수)
- **헬스체크**: 장시간 실행되는 프로세스의 상태를 주기적으로 확인하는 운영 패턴

---

## 7. 데이터 병합 및 CSV 변환 (Step 6)

### 7-1. 두 종류의 데이터 소스

우리 프로젝트에는 두 가지 출처의 데이터가 있었습니다:

| 데이터 소스 | 파일 | 특징 |
|-----------|------|------|
| **과거 CSV** | `pukyong_all_restaurants.csv` | 약 620개, 카테고리가 전부 "음식점"으로 뭉뚱그려짐 |
| **새 JSON** | `data/restaurants_chunk_1~3.json` | 150개, 카테고리가 정확하게 분류되어 있음 |

### 7-2. 병합 로직 (`upload_to_supabase.py`)

```python
# 1. JSON 파일들 읽기
json_files = glob.glob("data/restaurants_chunk_*.json")
for file_path in json_files:
    with open(file_path, 'r', encoding='utf-8') as f:
        data = json.load(f)
        json_records.extend(data.get('restaurants', []))

# 2. CSV 파일 읽기
df_csv = pd.read_csv("pukyong_all_restaurants.csv")

# 3. 이름(name) 기준으로 Outer Join 병합
df_merged = pd.merge(df_csv, df_json, on='name', how='outer', suffixes=('_csv', '_json'))

# 4. 데이터 소스 판별
def determine_source(row):
    has_csv = pd.notna(row.get('data_source_csv'))
    has_json = pd.notna(row.get('data_source_json'))
    if has_csv and has_json:
        return 'MERGED'   # 양쪽 다 있음
    elif has_csv:
        return 'CSV'      # CSV에만 있음
    else:
        return 'JSON'     # JSON에만 있음
```

### 7-3. 충돌 해결 전략

같은 식당이 CSV와 JSON 양쪽에 모두 있을 때, 어느 쪽 데이터를 우선할지 결정:

```python
# JSON 데이터를 우선하는 컬럼들 (더 정확하므로)
df_merged['category'] = combine_cols('category_csv', 'category_json', prefer_json=True)
df_merged['road_address'] = combine_cols('road_address_csv', 'road_address_json', prefer_json=True)

# CSV 데이터를 우선하는 컬럼들 (더 풍부하므로)
df_merged['star_rating'] = combine_cols('star_rating_csv', 'star_rating_json', prefer_json=False)
df_merged['visitor_reviews'] = combine_cols('visitor_reviews_csv', 'visitor_reviews_json', prefer_json=False)
```

### 💡 여기서 배울 점
- **Pandas merge (Outer Join)**: 두 데이터프레임을 특정 키 기준으로 합치되, 한쪽에만 있는 데이터도 보존
- **suffixes**: 같은 이름의 컬럼이 양쪽에 있을 때 `_csv`, `_json` 같은 접미사를 붙여 구분
- **데이터 충돌 해결**: 같은 필드에 대해 어떤 소스를 우선할지 비즈니스 로직으로 결정

---

## 8. 카테고리 재분류 작업 (Step 7)

### 8-1. 문제 상황

병합 후 CSV 파일을 열어보니, 과거 데이터에서 온 약 616개의 식당이 카테고리가 전부 **"음식점"**이라고만 표시되어 있었습니다.

### 8-2. 해결 Phase 1: 네이버 API 역추적 (`reclassify_categories.py`)

616개의 식당 이름을 하나씩 네이버 API에 다시 검색하여, 네이버가 분류한 원본 카테고리를 가져온 뒤 우리의 카테고리로 매핑했습니다.

```python
def map_api_category_to_custom(api_category_str):
    """
    네이버 원본: '음식점>한식>육류,고기요리'
    →  우리 카테고리: '고기'
    """
    parts = api_category_str.split(">")  # ['음식점', '한식', '육류,고기요리']
    
    # 세부 카테고리(뒷부분)부터 우선 매핑 시도
    for part in reversed(parts):
        sub_parts = part.split(",")
        for sub_part in sub_parts:
            for primary_cat, keywords in CATEGORY_MAPPING.items():
                if sub_part in keywords:
                    return primary_cat
    
    return "기타"
```

**결과**: 616개 중 **580개**가 성공적으로 매핑됨

### 8-3. 해결 Phase 2: 키워드 매칭 (`fix_remaining.py`)

네이버에서 검색이 안 되는 36개(폐업 등)는 식당 이름에 포함된 키워드로 추론:

```python
# 식당 이름 안에 우리 카테고리 키워드가 있으면 매핑
for primary_cat, keywords in config.CATEGORY_MAPPING.items():
    for keyword in keywords:
        if keyword in name:  # 예: "BBQ치킨" → "치킨" 키워드 발견!
            mapped_cat = primary_cat
            break
```

### 8-4. 해결 Phase 3: 수동 매핑 (`fix_gita.py`)

키워드로도 분류가 안 되는 애매한 이름들(예: "OL'55", "구디", "블루")은 하나하나 직접 조사해서 수동으로 매핑 사전을 만들었습니다.

```python
manual_mapping = {
    "OL'55": "양식",           # 바/펍 → 양식으로 분류
    "고기대학": "고기",         # 이름에 '고기'가 있지만 CATEGORY_MAPPING에는 없어서 수동
    "맘스터치 용호자이점": "햄버거",
    "김씨화덕 W스퀘어점": "피자",
    # ... 총 75개
}
```

### 8-5. 3단계 접근법 요약

```
Phase 1: 네이버 API 역추적  →  580개 해결  (자동)
Phase 2: 키워드 매칭         →   36개 해결  (반자동)
Phase 3: 수동 매핑 사전      →   75개 해결  (수동)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
최종 결과: "음식점" 또는 "기타" = 0개  ✅
```

### 💡 여기서 배울 점
- **점진적 해결(Iterative Approach)**: 한 번에 완벽하게 못 풀면, 단계를 나눠서 점점 정밀하게 접근
- **카테고리 매핑**: 외부 API의 분류 체계를 내부 분류 체계로 변환하는 데이터 정제 기법
- **Fallback 전략**: 1차 방법 실패 → 2차 방법 시도 → 3차 수동 처리
- **데이터 정제(Data Cleaning)**: 실제 데이터는 항상 지저분하고, 정제에 전체 작업의 80%가 소요됨

---

## 9. GitHub 동기화 (Step 8)

### 9-1. 기본 Git 명령어 흐름

```bash
# 1. 현재 상태 확인 (어떤 파일이 변경되었는지)
git status

# 2. 변경된 파일들을 스테이징 (보따리에 담기)
git add .                           # 모든 변경 파일 추가
git add merged_restaurants_result.csv   # 특정 파일만 추가

# 3. 커밋 (보따리 포장 + 라벨 붙이기)
git commit -m "머지된 csv파일 수정 및 관련된 파일 업로드"

# 4. 푸시 (깃허브에 올리기)
git push origin main
```

### 9-2. 흔한 에러: "rejected" (거절됨)

```
error: failed to push some refs to 'https://github.com/...'
hint: Updates were rejected because the remote contains work that you do not have locally.
```

이 에러는 **깃허브에 내가 모르는 새로운 변경사항이 있을 때** 발생합니다.

해결 방법:
```bash
# 1. 깃허브의 최신 내용을 먼저 받아서 합치기 (rebase 방식)
git pull --rebase origin main

# 2. 다시 푸시
git push origin main
```

### 9-3. Rebase vs Merge 차이

```
[Merge 방식] - git pull origin main
A - B - C      (내 작업)
         \
          M    (병합 커밋이 하나 더 생김)
         /
    D - E      (깃허브 작업)

[Rebase 방식] - git pull --rebase origin main  ← 우리가 사용한 방식
D - E - A' - B' - C'   (깃허브 작업 위에 내 작업을 다시 쌓음)
→ 히스토리가 깔끔한 직선이 됨!
```

### 💡 여기서 배울 점
- **Git 기본 흐름**: status → add → commit → push
- **Pull 충돌 해결**: 원격에 새 변경이 있으면 먼저 pull 후 push
- **Rebase**: 커밋 히스토리를 깔끔하게 유지하는 고급 기법

---

## 10. 사용된 핵심 기술 정리

| 기술 | 용도 | 파일 |
|------|------|------|
| `requests` | HTTP API 호출 | collector.py |
| `requests.Session` | 연결 재사용 + 자동 재시도 | collector.py |
| `urllib3.Retry` | 429 에러 시 자동 백오프 | collector.py |
| `pandas` | CSV/DataFrame 처리 | upload_to_supabase.py, reclassify_categories.py |
| `json` | JSON 파일 읽기/쓰기 | collector.py |
| `python-dotenv` | .env 파일에서 API 키 로드 | config.py |
| `hashlib` | 식당 고유 ID 생성 (MD5 해시) | collector.py |
| `glob` | 파일 패턴 매칭 (chunk_*.json) | collector.py, upload_to_supabase.py |
| `haversine` | GPS 좌표 간 거리 계산 | utils.py |
| `Git` | 버전 관리 및 GitHub 동기화 | 터미널 |

---

## 11. 파일 구조 총정리

```
Naver 크롤링 & API/
├── .env                          # 🔒 API 키 (절대 깃허브에 올리면 안 됨!)
├── .gitignore                    # Git이 무시할 파일 목록
│
├── config.py                     # ⚙️ 전역 설정 (API URL, 좌표, 카테고리 매핑)
├── collector.py                  # 🤖 메인 크롤러 (네이버 API 호출 + 필터링 + 저장)
├── utils.py                      # 🛠️ 유틸리티 함수 (좌표 변환, 거리 계산, HTML 정리)
│
├── data/                         # 📂 수집된 JSON 데이터
│   ├── restaurants_chunk_1.json  # 1~50번째 식당
│   ├── restaurants_chunk_2.json  # 51~100번째 식당
│   └── restaurants_chunk_3.json  # 101~150번째 식당
│
├── upload_to_supabase.py         # 🔀 CSV + JSON 병합 스크립트
├── export_to_csv.py              # 📤 병합 결과를 CSV로 내보내기
│
├── reclassify_categories.py      # 🏷️ Phase 1: 네이버 API로 카테고리 역추적
├── fix_remaining.py              # 🏷️ Phase 2: 키워드 매칭으로 나머지 처리
├── fix_gita.py                   # 🏷️ Phase 3: 수동 매핑으로 최종 마무리
│
├── merged_restaurants_result.csv # 🎯 최종 결과물! (767개 식당, 12개 카테고리)
├── pukyong_all_restaurants.csv   # 📜 과거 수집 데이터 (원본 보존)
├── pukyong_restaurants_800m.csv  # 📜 과거 수집 데이터 (800m 필터링 버전)
│
├── collector_v14.log             # 📝 크롤러 실행 로그 (1차)
├── collector_v14_resume.log      # 📝 크롤러 실행 로그 (이어서 실행)
├── reclassify.log                # 📝 카테고리 재분류 실행 로그
│
├── cats.txt                      # ✅ 카테고리 검증용 (12개 카테고리 목록)
├── gita_names.txt                # 📋 '기타'로 분류된 식당 이름 목록
├── check_cats.py                 # 🔍 카테고리 검증 스크립트
└── dump_gita.py                  # 🔍 '기타' 항목 추출 스크립트
```

---

## 🔑 핵심 교훈 요약

1. **API Rate Limit은 반드시 온다** → 반드시 재시도/백오프 로직을 미리 구현해 놓을 것
2. **실제 데이터는 항상 지저분하다** → 데이터 정제(Cleaning)에 전체 시간의 80%가 소요됨
3. **한 번에 완벽하게 못 풀면 단계를 나눠라** → Phase 1(자동) → Phase 2(반자동) → Phase 3(수동)
4. **청크 저장으로 안전망을 만들어라** → 중간에 프로그램이 죽어도 데이터 유실 방지
5. **로그는 무조건 남겨라** → 문제가 생겼을 때 원인 파악의 유일한 단서
6. **Git은 자주 커밋하라** → 작은 단위로 자주 저장하면 되돌리기도 쉬움

---

## 12. Windows 환경 트러블슈팅

Windows에서 파이썬 크롤러를 돌리면 예상치 못한 문제들이 쏟아집니다. 우리가 실제로 마주친 문제들과 해결법을 정리합니다.

### 12-1. 한글 깨짐 문제 (인코딩)

Windows 콘솔의 기본 인코딩은 **CP949(EUC-KR)**입니다. 하지만 우리 코드는 **UTF-8**을 사용합니다. 이 불일치 때문에 콘솔에 한글이 `?????` 또는 `깨진문자`로 표시됩니다.

**해결 방법 1: 파이썬 코드 내에서 강제 UTF-8 설정**
```python
import sys
import io

# 콘솔 출력 인코딩을 UTF-8로 강제 전환
if sys.stdout.encoding != "utf-8":
    sys.stdout = io.TextIOWrapper(
        sys.stdout.buffer,
        encoding="utf-8",
        errors="replace",       # 변환 불가능한 문자는 '?'로 대체
        line_buffering=True     # 줄바꿈마다 즉시 출력
    )
```

**해결 방법 2: 로그 파일로 리다이렉트**
콘솔 출력 대신 파일에 저장하면 인코딩 문제를 아예 피할 수 있습니다.
```bash
python -u collector.py >> collector.log 2>&1
```
하지만 이때 `>>` 리다이렉트는 Windows의 기본 인코딩(CP949)을 사용하므로, 로그 파일도 깨질 수 있습니다. 가장 확실한 방법은 **파이썬 코드 안에서 직접 파일 핸들로 로그를 쓰는 것**입니다.

### 12-2. CSV 파일의 한글 깨짐 (BOM 문제)

CSV 파일을 `utf-8`로 저장하면 **엑셀에서 열 때** 한글이 깨집니다.
엑셀은 CSV 파일을 열 때 BOM(Byte Order Mark)이 없으면 시스템 기본 인코딩(CP949)으로 해석하기 때문입니다.

**해결법: `utf-8-sig` 인코딩 사용**
```python
# 이렇게 하면 CSV 파일 맨 앞에 BOM(\xef\xbb\xbf)이 자동으로 추가됨
df.to_csv("output.csv", index=False, encoding='utf-8-sig')
```

| 인코딩 | BOM 포함 | 엑셀 호환 | 프로그래밍 호환 |
|--------|---------|----------|--------------|
| `utf-8` | ❌ | ❌ 한글 깨짐 | ✅ 완벽 |
| `utf-8-sig` | ✅ | ✅ 완벽 | ✅ 완벽 |
| `cp949` | ❌ | ✅ | ❌ 영문만 |

### 12-3. UnicodeEncodeError 에러

```
UnicodeEncodeError: 'cp949' codec can't encode character '\u2705' in position 2
```

이 에러는 ✅ 같은 **이모지(Emoji)**를 `print()`로 출력할 때 발생합니다. CP949 인코딩은 이모지를 지원하지 않기 때문입니다.

**해결법**: 위의 12-1에서 설명한 UTF-8 강제 설정을 적용하거나, print문에서 이모지를 제거합니다.

### 12-4. SSL 인증서 경고 (InsecureRequestWarning)

우리가 DNS 최적화를 위해 IP 주소를 직접 사용하면, SSL 인증서의 도메인과 실제 접속 주소가 달라져서 경고가 발생합니다.

```python
import urllib3
urllib3.disable_warnings(urllib3.exceptions.InsecureRequestWarning)

# API 호출 시 SSL 검증 비활성화
response = api_session.get(url, verify=False)
```

> ⚠️ **주의**: `verify=False`는 보안상 좋지 않은 방법입니다. 프로덕션 환경에서는 절대 사용하지 마세요. 우리는 네이버 API라는 신뢰할 수 있는 서버에 접속하는 것이 확실하고, 개발/크롤링 용도이므로 사용한 것입니다.

### 💡 여기서 배울 점
- **인코딩(Encoding)**: 컴퓨터가 문자를 숫자로 변환하는 규칙. UTF-8은 전 세계 문자를, CP949는 한국어만 지원
- **BOM(Byte Order Mark)**: 파일의 인코딩 방식을 알려주는 바이트 시퀀스
- **SSL(Secure Socket Layer)**: HTTPS 통신에서 데이터를 암호화하는 보안 프로토콜

---

## 13. Python 핵심 개념 딥다이브

이 프로젝트에서 사용된 파이썬 핵심 개념들을 깊이 있게 설명합니다.

### 13-1. Context Manager (`with` 문)

파일이나 네트워크 연결 같은 **자원(Resource)**은 사용 후 반드시 닫아줘야 합니다. `with` 문을 사용하면 자동으로 정리됩니다.

```python
# ❌ 나쁜 방법 (파일을 직접 열고 닫아야 함)
f = open("data.json", "r", encoding="utf-8")
data = json.load(f)
f.close()  # 이걸 깜빡하면 메모리 누수!

# ✅ 좋은 방법 (with 문 사용 - 자동으로 닫힘)
with open("data.json", "r", encoding="utf-8") as f:
    data = json.load(f)
# 여기서 자동으로 f.close() 호출됨

# requests에서도 사용 가능
with api_session.get(url, headers=headers) as response:
    data = response.json()
```

### 13-2. 리스트 컴프리헨션 (List Comprehension)

반복문을 한 줄로 축약하는 파이썬만의 문법입니다.

```python
# ❌ 일반 방법 (4줄)
results = []
for item in items:
    if "음식점" in item.get("category", ""):
        results.append(item["title"])

# ✅ 리스트 컴프리헨션 (1줄)
results = [item["title"] for item in items if "음식점" in item.get("category", "")]
```

### 13-3. 딕셔너리의 `.get()` 메서드

딕셔너리에서 키가 없을 때 에러 대신 기본값을 돌려줍니다.

```python
item = {"title": "홍콩반점", "category": "중식"}

# ❌ 위험한 방법 (키가 없으면 KeyError 발생!)
phone = item["phone"]  # KeyError!

# ✅ 안전한 방법 (.get()은 키가 없으면 기본값 반환)
phone = item.get("phone", "")  # 키 없으면 빈 문자열 "" 반환
phone = item.get("phone")       # 키 없으면 None 반환
```

### 13-4. f-string (포맷 문자열)

변수를 문자열 안에 직접 삽입하는 현대적 방법입니다.

```python
name = "홍콩반점"
distance = 0.85
rating = 4.5

# ❌ 옛날 방법
print("식당: %s, 거리: %.1fkm" % (name, distance))
print("식당: {}, 거리: {:.1f}km".format(name, distance))

# ✅ f-string (Python 3.6+, 가장 읽기 쉬움)
print(f"식당: {name}, 거리: {distance:.1f}km, 별점: {rating}")

# f-string 안에서 연산도 가능
print(f"거리: {distance * 1000:.0f}m")  # → "거리: 850m"
```

### 13-5. 예외 처리 (try/except)

API 호출은 언제든 실패할 수 있습니다. 에러가 나도 프로그램이 죽지 않도록 예외를 처리합니다.

```python
try:
    response = api_session.get(url, timeout=(5.0, 15.0))
    response.raise_for_status()  # 4xx, 5xx 에러 시 예외 발생
    data = response.json()

except requests.exceptions.HTTPError as e:
    # HTTP 에러 (400, 401, 429, 500 등)
    if response.status_code == 429:
        print("Rate Limit! 5초 대기...")
        time.sleep(5)
    elif response.status_code == 401:
        print("API 키가 잘못되었습니다!")

except requests.exceptions.ConnectionError:
    # 네트워크 연결 실패
    print("인터넷 연결을 확인해주세요.")

except requests.exceptions.Timeout:
    # 응답 시간 초과
    print("서버 응답이 너무 느립니다.")

except Exception as e:
    # 예상치 못한 모든 에러 (최후의 안전망)
    print(f"알 수 없는 에러: {e}")
```

### 13-6. Pandas 핵심 메서드

```python
import pandas as pd

# 1. CSV 파일 읽기/쓰기
df = pd.read_csv("data.csv")                              # 읽기
df.to_csv("output.csv", index=False, encoding="utf-8-sig") # 쓰기

# 2. 특정 조건으로 행 필터링
mask = df['category'] == '음식점'    # 조건 마스크 생성
filtered = df[mask]                  # 마스크로 필터링
indices = df[mask].index             # 조건에 해당하는 행 번호 목록

# 3. 특정 셀 값 읽기/쓰기
value = df.at[idx, 'category']       # idx번째 행의 'category' 값 읽기
df.at[idx, 'category'] = '한식'      # idx번째 행의 'category' 값 쓰기

# 4. 고유값(Unique) 확인
unique_cats = df['category'].unique()           # 중복 없는 값 목록
unique_sorted = sorted(df['category'].unique()) # 정렬된 목록

# 5. 두 데이터프레임 병합 (SQL의 JOIN과 같음)
merged = pd.merge(df1, df2, on='name', how='outer', suffixes=('_csv', '_json'))
# on: 병합 기준 컬럼
# how: 'inner'(교집합), 'outer'(합집합), 'left', 'right'
# suffixes: 같은 이름 컬럼 구분용 접미사

# 6. 결측값(NaN) 처리
df['phone'].fillna("")               # NaN을 빈 문자열로 채움
df['category'].isna()                 # NaN인지 True/False 반환
pd.notna(value)                       # None/NaN이 아니면 True
```

### 13-7. hashlib을 이용한 고유 ID 생성

```python
import hashlib

# 식당 이름 + 주소를 조합하여 고유한 8자리 ID 생성
raw_id = f"홍콩반점_부산 남구 대연동 55-1"
rest_id = "rest_" + hashlib.md5(raw_id.encode("utf-8")).hexdigest()[:8]
# 결과: "rest_a1b2c3d4"

# 왜 MD5 해시를 사용하나?
# → 같은 입력은 항상 같은 출력 = 중복 방지에 완벽
# → 다른 입력은 거의 100% 다른 출력 = 충돌 거의 없음
# → [:8]로 앞 8자리만 사용 = 간결한 ID
```

### 13-8. glob 패턴 매칭

파일 시스템에서 특정 패턴에 맞는 파일들을 찾습니다.

```python
import glob

# data/ 디렉토리에서 "restaurants_chunk_숫자.json" 패턴의 파일 모두 찾기
json_files = glob.glob("data/restaurants_chunk_*.json")
# 결과: ['data/restaurants_chunk_1.json', 'data/restaurants_chunk_2.json', ...]

# * : 아무 문자열 (0개 이상)
# ? : 아무 문자 1개
# [0-9] : 숫자 1개
```

### 💡 여기서 배울 점
- **Context Manager**: 리소스를 안전하게 열고 닫는 파이썬 패턴
- **방어적 프로그래밍**: `.get()`, `try/except` 등으로 에러에 대비
- **Pandas**: 표 형태 데이터 처리의 사실상 표준 라이브러리
- **해시 함수**: 임의의 데이터를 고정 길이 문자열로 변환하는 단방향 함수

---

## 14. 전체 데이터 흐름 아키텍처

이 프로젝트의 전체 데이터 흐름을 시각적으로 표현합니다.

### 14-1. 크롤링 단계 흐름

```
┌─────────────────────────────────────────────────────────────────────┐
│                        크롤링 단계 (collector.py)                     │
│                                                                     │
│  config.py                                                          │
│  ┌─────────────┐     ┌──────────────┐     ┌──────────────────┐     │
│  │ 행정동 14개  │  ×  │ 카테고리 198개│  =  │ 검색 쿼리 2,772개 │     │
│  └─────────────┘     └──────────────┘     └────────┬─────────┘     │
│                                                     │               │
│                                                     ▼               │
│                                           ┌─────────────────┐      │
│                                           │  네이버 Local    │      │
│                                           │  Search API 호출 │      │
│                                           └────────┬────────┘      │
│                                                     │               │
│                         ┌───────────────────────────┼──────┐       │
│                         │        3단계 필터링         │      │       │
│                         │  ① 카테고리 필터 (음식점?)  │      │       │
│                         │  ② 중복 필터 (이미 있는?)   │      │       │
│                         │  ③ 거리 필터 (1km 이내?)    │      │       │
│                         └───────────────┬────────────┘       │       │
│                                          │ 통과!              │       │
│                                          ▼                   │       │
│                                  ┌──────────────┐           │       │
│                                  │  이미지 API   │           │       │
│                                  │  추가 호출    │           │       │
│                                  └──────┬───────┘           │       │
│                                          │                   │       │
│                                          ▼                   │       │
│                                  ┌──────────────┐           │       │
│                                  │ 청크 버퍼     │           │       │
│                                  │ (50개씩 모음) │           │       │
│                                  └──────┬───────┘           │       │
│                                          │ 50개 도달!        │       │
│                                          ▼                   │       │
│                                  ┌──────────────────┐       │       │
│                                  │ chunk_N.json 저장 │       │       │
│                                  └──────────────────┘       │       │
└─────────────────────────────────────────────────────────────────────┘
```

### 14-2. 데이터 정제 단계 흐름

```
┌──────────────────────────────────────────────────────────────────┐
│                    데이터 정제 단계                                │
│                                                                  │
│  ┌───────────────────┐     ┌────────────────────┐               │
│  │ pukyong_all_       │     │ data/restaurants_   │               │
│  │ restaurants.csv    │     │ chunk_1~3.json      │               │
│  │ (620개, 카테고리   │     │ (150개, 카테고리    │               │
│  │  = "음식점")       │     │  = "한식" 등 정확)  │               │
│  └────────┬──────────┘     └────────┬───────────┘               │
│           │                          │                           │
│           └──────────┬───────────────┘                           │
│                      ▼                                           │
│            ┌──────────────────┐                                  │
│            │ upload_to_       │                                  │
│            │ supabase.py      │  ← Pandas merge (Outer Join)    │
│            │ 데이터 병합       │                                  │
│            └────────┬─────────┘                                  │
│                      ▼                                           │
│            ┌──────────────────┐                                  │
│            │ export_to_csv.py │                                  │
│            │ CSV 내보내기      │                                  │
│            └────────┬─────────┘                                  │
│                      ▼                                           │
│            ┌──────────────────────┐                              │
│            │ merged_restaurants_   │                              │
│            │ result.csv            │  ← 이 시점에서 616개가      │
│            │ (767개, 일부 "음식점")│    여전히 "음식점"            │
│            └────────┬─────────────┘                              │
│                      │                                           │
│     ┌────────────────┼────────────────┐                          │
│     ▼                ▼                ▼                          │
│ Phase 1          Phase 2          Phase 3                        │
│ reclassify_      fix_remaining    fix_gita                       │
│ categories.py    .py              .py                            │
│ (네이버 API      (키워드 매칭)    (수동 매핑)                     │
│  역추적)                                                         │
│ 580개 해결       36개 해결        75개 해결                       │
│     │                │                │                          │
│     └────────────────┼────────────────┘                          │
│                      ▼                                           │
│            ┌──────────────────────┐                              │
│            │ merged_restaurants_   │                              │
│            │ result.csv            │  ← 최종 완성본!             │
│            │ (767개, 12개 카테고리)│    "음식점" = 0개            │
│            └──────────────────────┘                              │
└──────────────────────────────────────────────────────────────────┘
```

### 💡 여기서 배울 점
- **파이프라인 설계**: 데이터가 여러 단계를 거쳐 점진적으로 정제되는 구조
- **관심사의 분리(SoC)**: 수집(collector) → 병합(upload) → 정제(reclassify) → 출력(export) 를 각각 별도 파일로 분리
- **멱등성(Idempotency)**: 같은 스크립트를 여러 번 실행해도 결과가 동일해야 함 (중복 체크 덕분에 가능)

---

## 15. utils.py 코드 완전 해부

`utils.py`는 프로젝트 전체에서 공통으로 사용하는 유틸리티 함수들의 모음입니다.

### 15-1. Haversine 공식 (거리 계산) - 수학 원리

두 GPS 좌표(위도/경도) 사이의 실제 지구 표면 거리를 계산하는 공식입니다.

```
수학 공식:
a = sin²(Δlat/2) + cos(lat1) × cos(lat2) × sin²(Δlng/2)
c = 2 × atan2(√a, √(1-a))
d = R × c

여기서:
- R = 6,371 km (지구 반지름)
- Δlat = lat2 - lat1 (위도 차이, 라디안 변환 필요)
- Δlng = lng2 - lng1 (경도 차이, 라디안 변환 필요)
```

파이썬 구현:
```python
import math

def haversine_distance(lat1, lng1, lat2, lng2):
    R = 6371  # 지구 반지름 (km)

    # 1단계: 도(degree) → 라디안(radian) 변환
    #   라디안 = 도 × (π / 180)
    #   왜? 삼각함수(sin, cos)는 라디안을 입력으로 받기 때문
    lat1_r = math.radians(lat1)   # 예: 35.134 → 0.6134...
    lat2_r = math.radians(lat2)
    dlat = math.radians(lat2 - lat1)
    dlng = math.radians(lng2 - lng1)

    # 2단계: Haversine 공식의 핵심 계산
    a = (math.sin(dlat / 2) ** 2 +
         math.cos(lat1_r) * math.cos(lat2_r) * math.sin(dlng / 2) ** 2)

    # 3단계: 중심각 계산
    c = 2 * math.atan2(math.sqrt(a), math.sqrt(1 - a))

    # 4단계: 호의 길이 = 반지름 × 중심각
    return R * c  # km 단위
```

**실제 사용 예시:**
```python
# 부경대학교 좌표
school_lat, school_lng = 35.1340802, 129.1031735

# 어떤 식당의 좌표
rest_lat, rest_lng = 35.1370, 129.1020

distance = haversine_distance(rest_lat, rest_lng, school_lat, school_lng)
print(f"거리: {distance:.2f}km")  # → "거리: 0.34km"

# 1km 이내인지 확인
if distance <= 1.0:
    print("반경 내!")
```

### 15-2. HTML 태그 제거 (정규표현식)

```python
import re

def clean_html_tags(text):
    """
    네이버 API 응답: "<b>홍콩</b>반점 경성대점"
    → 정제 후:       "홍콩반점 경성대점"
    """
    # 정규표현식: < 로 시작하고 > 로 끝나는 모든 것을 빈 문자열로 치환
    clean = re.sub(r"<[^>]+>", "", text)

    # r"<[^>]+>" 해석:
    # r"..."  : 원시 문자열 (역슬래시를 특수 문자로 해석 안 함)
    # <       : 꺾쇠 여는 괄호 그대로 매칭
    # [^>]+   : '>'가 아닌 문자가 1개 이상 연속  
    # >       : 꺾쇠 닫는 괄호 그대로 매칭
    # 결과: <b>, </b>, <span class="...">, </span> 등 모든 태그 매칭

    # 연속 공백을 하나로 정리
    clean = re.sub(r"\s+", " ", clean).strip()
    return clean
```

### 15-3. 이름 정규화

중복 체크의 정확도를 높이기 위해 이름을 표준 형태로 변환합니다.

```python
def normalize_name(name):
    """
    입력: "홍콩 반점 0410  "
    출력: "홍콩반점0410"
    """
    name = clean_html_tags(name)  # HTML 태그 제거
    name = name.replace(" ", "")  # 공백 모두 제거
    name = name.lower()           # 소문자로 변환
    return name
```

왜 정규화가 필요한가?
```
"BBQ치킨 경성대점" 과 "BBQ 치킨  경성대점"은 같은 가게인데,
문자열 비교(==)로는 다르다고 판단함.
정규화하면 둘 다 "bbq치킨경성대점"이 되어 같다고 판단!
```

### 💡 여기서 배울 점
- **정규표현식(Regex)**: 텍스트에서 패턴을 찾고 치환하는 강력한 도구
- **좌표계 변환**: 서로 다른 좌표 체계 간의 변환이 지도 서비스에서 매우 중요
- **데이터 정규화**: 동일한 데이터를 통일된 형태로 변환하여 비교/검색 정확도 향상

---

## 16. 실제 디버깅 사례 모음

프로젝트를 진행하면서 실제로 발생했던 문제들과 해결 과정을 기록합니다. 디버깅 능력은 코딩 실력의 핵심입니다!

### 사례 1: 네이버 API가 쓰레기 데이터를 반환

**증상**: "대연동 한식"을 검색했는데 화장품 가게, 부동산, 복권 판매점 같은 엉뚱한 결과가 나옴

**원인**: 네이버 지역 검색 API는 키워드와 관련된 *모든 업종*의 결과를 반환함. 검색어에 "한식"이 들어가 있어도 주변의 전혀 관계없는 업체가 함께 나올 수 있음.

**해결**: API 응답의 `category` 필드를 검사하여 "음식점", "카페" 등이 포함된 결과만 통과시키는 화이트리스트 필터 추가.

```python
valid_categories = ["음식점", "카페", "주점", "간식", "베이커리", "패스트푸드", "뷔페"]
if not any(valid in api_category for valid in valid_categories):
    continue  # 통과 실패! 다음 결과로
```

---

### 사례 2: git push rejected (거절됨)

**증상**: `git push origin main` 시 "rejected" 에러 발생

```
error: failed to push some refs to 'https://github.com/...'
hint: Updates were rejected because the remote contains work that you do not have locally.
```

**원인**: 깃허브(원격 저장소)에 새로운 커밋이 추가되었는데, 로컬에서 그 변경사항을 아직 받아오지 않은 상태에서 push를 시도한 것.

**해결**: 먼저 `pull --rebase`로 원격의 최신 변경사항을 가져온 후 다시 push.

```bash
git pull --rebase origin main   # 원격 변경사항 위에 내 커밋을 재배치
git push origin main            # 다시 push
```

**교훈**: push 전에 항상 `git pull`을 먼저 습관화하자!

---

### 사례 3: 카테고리 "음식점" 문제가 재발

**증상**: 네이버 API로 580개를 재분류 했는데 36개가 여전히 "음식점"으로 남아있음

**원인**: 해당 36개 식당은 폐업했거나 네이버 지도에서 삭제되어 API 검색 결과가 0건이었음. 검색 실패 시 카테고리를 업데이트하지 않고 넘어가도록 코드가 작성되어 있었음.

**해결**: 2차 패스(키워드 매칭) + 3차 패스(수동 매핑)를 추가하여 남은 것들도 모두 처리.

**교훈**: 자동화 코드에는 반드시 "실패 시 대안(Fallback)"을 마련해두어야 함. 특히 외부 API에 의존하는 로직은 API가 결과를 못 돌려줄 경우를 항상 고려해야 함.

---

### 사례 4: 중복 식당이 계속 쌓이는 문제

**증상**: 크롤러를 중단했다가 재시작하니 이미 수집한 식당이 다시 수집됨

**원인**: 크롤러 재시작 시 `seen_names` 세트(Set)가 비어있는 상태로 시작되어 이전에 수집한 식당들을 중복으로 인식하지 못함.

**해결**: 크롤러 시작 시 기존 청크 파일들을 모두 읽어서 `seen_names`를 복원하는 로직 추가.

```python
# 기존 청크 데이터에서 seen_names 복원 (중복 방지)
import glob
for chunk_file in glob.glob(os.path.join(DATA_DIR, "restaurants_chunk_*.json")):
    try:
        with open(chunk_file, "r", encoding="utf-8") as f:
            data = json.load(f)
            for r in data.get("restaurants", []):
                seen_names.add(r.get("name_normalized", ""))
    except Exception:
        pass
```

---

### 사례 5: 같은 카테고리 키워드가 잘못된 대분류로 매핑

**증상**: "바로그김밥"이라는 식당이 "햄버거"로 분류됨

**원인**: `fix_remaining.py`에서 식당 이름에 포함된 키워드를 순서대로 검색하는데, `CATEGORY_MAPPING`의 "햄버거" 카테고리에 "서브웨이" 키워드가 있었고, 우연히 다른 키워드와 겹쳐서 먼저 매칭됨. 실제로는 "김밥" → "분식"이 맞음.

**교훈**: 키워드 매칭은 **더 구체적인(긴) 키워드를 먼저 검사**해야 오매칭을 줄일 수 있음. 또는 매칭 우선순위를 명확히 정의해야 함.

---

### 💡 디버깅 꿀팁 모음

1. **에러 메시지를 끝까지 읽어라**: 대부분의 에러 메시지는 원인과 해결 힌트를 포함하고 있음
2. **print() 디버깅을 부끄러워하지 마라**: 복잡한 디버거보다 `print(f"여기 통과: {변수값}")` 가 빠를 때가 많음
3. **작은 단위로 테스트하라**: 2,772개 전체를 돌리기 전에 1개만 먼저 돌려보기
4. **로그를 남겨라**: `>> log.txt 2>&1` 로 모든 출력을 파일에 저장해두면 나중에 원인 추적 가능
5. **Git을 자주 커밋하라**: 뭔가 고장 나면 `git diff` 로 뭐가 바뀌었는지 확인 가능

---

## 17. 처음부터 재현하기 (From Scratch Tutorial)

이 프로젝트를 0부터 다시 만들고 싶다면, 아래 순서를 따르세요.

### Step 1: 프로젝트 폴더 생성

```bash
mkdir "부경대 맛집 크롤러"
cd "부경대 맛집 크롤러"
git init
```

### Step 2: 파이썬 가상환경 및 패키지 설치

```bash
# 가상환경 생성 (선택사항이지만 강력 추천)
python -m venv venv
venv\Scripts\activate        # Windows
# source venv/bin/activate   # Mac/Linux

# 필수 패키지 설치
pip install requests pandas python-dotenv urllib3
```

### Step 3: .env 파일에 API 키 등록

```bash
# .env 파일 생성
echo NAVER_CLIENT_ID=여기에_발급받은_ID > .env
echo NAVER_CLIENT_SECRET=여기에_발급받은_SECRET >> .env
```

### Step 4: .gitignore 설정

```bash
# .gitignore 파일 생성 (민감 정보 보호)
echo .env > .gitignore
echo __pycache__/ >> .gitignore
echo venv/ >> .gitignore
echo *.log >> .gitignore
```

### Step 5: 설정 파일 작성 (config.py)

핵심 포인트:
- 학교 좌표를 정확하게 입력 (Google Maps에서 확인 가능)
- 검색 반경 설정 (1km 추천)
- 카테고리 매핑을 최대한 촘촘하게 작성 (빈틈이 있으면 데이터 누락)

### Step 6: 유틸리티 함수 작성 (utils.py)

핵심 포인트:
- `katec_to_wgs84()`: 네이버 좌표 변환
- `haversine_distance()`: GPS 거리 계산
- `clean_html_tags()`: API 응답 정제
- `normalize_name()`: 중복 체크용 이름 표준화

### Step 7: 크롤러 작성 (collector.py)

핵심 포인트:
- `requests.Session` + `Retry` 설정 (429 방어)
- 3단계 필터링 파이프라인 구현
- 청크 저장 방식 (50개마다 파일 저장)
- `--test` 모드 지원 (1개 카테고리만 테스트)

### Step 8: 테스트 실행

```bash
# 먼저 1개 카테고리만 테스트
python collector.py --test

# 정상 작동하면 전체 크롤링 시작 (백그라운드)
python -u collector.py >> crawler.log 2>&1
```

### Step 9: 데이터 정제 및 CSV 내보내기

```bash
# 병합 + CSV 변환
python export_to_csv.py

# 카테고리 재분류 (필요 시)
python reclassify_categories.py
```

### Step 10: GitHub에 올리기

```bash
git add .
git commit -m "초기 버전: 크롤러 + 데이터 수집 완료"
git remote add origin https://github.com/사용자명/저장소명.git
git push -u origin main
```

### 💡 여기서 배울 점
- **가상환경(venv)**: 프로젝트마다 독립된 패키지 환경을 만들어 충돌 방지
- **`.gitignore`**: Git이 추적하지 않을 파일/폴더 목록 (API 키, 로그, 캐시 등)
- **테스트 모드**: 전체 실행 전에 작은 규모로 먼저 테스트하는 습관

---

## 📚 추가 학습 자료

더 깊이 공부하고 싶다면:

| 주제 | 추천 자료 |
|------|----------|
| REST API 기초 | MDN Web Docs - "HTTP 요청 메서드" |
| Git/GitHub 사용법 | 생활코딩 - "지옥에서 온 Git" |
| Pandas 데이터 분석 | 10 Minutes to Pandas (공식 문서) |
| 정규표현식 | RegExr.com (실시간 테스트 도구) |
| 파이썬 크롤링 | "파이썬으로 웹 크롤러 만들기" (점프 투 파이썬) |
| 네이버 API | 네이버 클라우드 플랫폼 공식 가이드 |

---

## 18. 용어 사전: 비전공자를 위한 모든 용어 정리

이 프로젝트에서 등장하는 모든 기술 용어를 **일상 비유**와 함께 정리했습니다. 모르는 단어가 나올 때마다 여기로 돌아와서 확인하세요!

### 🅰️ 기본 프로그래밍 용어

| 용어 | 일상 비유 | 설명 |
|------|----------|------|
| **변수 (Variable)** | 이름표 붙은 상자 📦 | 데이터를 담아두는 공간. `name = "홍콩반점"` 이라고 쓰면, `name`이라는 상자에 "홍콩반점"이라는 글자를 넣는 것 |
| **함수 (Function)** | 자판기 🥤 | 동전(입력)을 넣으면 음료(결과)가 나오는 것처럼, 데이터를 넣으면 처리 결과를 돌려주는 코드 묶음 |
| **반복문 (Loop)** | 세탁기의 회전 🔄 | 같은 동작을 여러 번 반복하는 것. `for`문으로 2,772개의 검색어를 하나씩 처리 |
| **조건문 (if/else)** | 갈림길 🔀 | "만약 거리가 1km 이내이면 → 저장, 아니면 → 버림" 처럼 조건에 따라 다른 동작 수행 |
| **리스트 (List)** | 줄 서있는 사람들 🧑‍🤝‍🧑 | 여러 데이터를 순서대로 나열한 것. `["한식", "일식", "중식"]` |
| **딕셔너리 (Dictionary)** | 사전/전화번호부 📖 | 키-값 쌍으로 데이터를 저장. `{"이름": "홍콩반점", "전화": "051-621-0410"}` |
| **모듈/라이브러리 (Module)** | 레고 블록 🧱 | 다른 사람이 미리 만들어 놓은 코드 묶음. `import pandas`로 가져다 씀 |
| **클래스 (Class)** | 설계도 📐 | 객체(실물)를 만들기 위한 틀. 붕어빵틀(클래스)로 붕어빵(객체)을 찍어냄 |

### 🌐 API / 네트워크 용어

| 용어 | 일상 비유 | 설명 |
|------|----------|------|
| **API** | 식당 주문 시스템 🍽️ | 메뉴판(문서)을 보고, 주문(요청)을 넣으면, 음식(데이터)이 나오는 것. 프로그램끼리 데이터를 주고받는 약속된 방법 |
| **HTTP 요청 (Request)** | 편지 보내기 ✉️ | 서버에게 "이 데이터 줘!"라고 요청하는 것. GET(조회), POST(등록), PUT(수정), DELETE(삭제) 등이 있음 |
| **HTTP 응답 (Response)** | 답장 받기 📬 | 서버가 요청에 대해 돌려주는 결과. 성공(200), 인증 실패(401), 차단(429), 서버 오류(500) 등 |
| **JSON** | 택배 포장 📦 | 데이터를 주고받을 때 사용하는 형식. `{"name": "홍콩반점", "category": "중식"}` 이런 모양 |
| **CSV** | 엑셀 파일 📊 | 쉼표(,)로 구분된 표 형태의 데이터 파일. 엑셀로 열 수 있음 |
| **엔드포인트 (Endpoint)** | 택배 보내는 주소 🏠 | API 서버의 특정 URL. 예: `/search/v1/local` (지역 검색용 주소) |
| **헤더 (Header)** | 택배 송장 🏷️ | HTTP 요청에 붙이는 추가 정보. API 키(인증 정보)를 여기에 담아 보냄 |
| **파라미터 (Parameter)** | 주문서 상세 내역 📝 | 요청할 때 같이 보내는 세부 조건. `query=대연동 한식`, `display=5` 등 |
| **Rate Limit (429 에러)** | 뷔페에서 "한 번에 3접시까지만!" 🚫 | 서버가 "너무 빠르게 요청하지 마!"라고 일시적으로 차단하는 것 |
| **SSL / HTTPS** | 택배 보안 봉투 🔒 | 인터넷에서 데이터를 암호화하여 도청을 방지하는 보안 기술 |
| **DNS** | 전화번호부 📱 | 도메인 이름(naver.com)을 실제 서버 주소(IP: 125.209.222.141)로 변환해주는 시스템 |
| **Session** | 단골 고객 카드 💳 | 한번 연결을 맺으면 재사용하여 매번 새로 연결하는 시간을 줄이는 기법 |
| **Timeout** | 배달 제한 시간 ⏰ | "5초 안에 응답 안 오면 포기하겠다"는 최대 대기 시간 설정 |

### 📁 파일 / 데이터 용어

| 용어 | 일상 비유 | 설명 |
|------|----------|------|
| **인코딩 (Encoding)** | 번역 규칙 🌍 | 컴퓨터가 글자를 숫자로 바꾸는 규칙. UTF-8(전 세계 언어 지원), CP949(한국어 전용) 등이 있음 |
| **BOM (Byte Order Mark)** | 책 표지에 적힌 언어 표시 🏴 | 파일 맨 앞에 "이 파일은 UTF-8이야!"라고 알려주는 특수 바이트. 엑셀이 한글을 읽으려면 이게 필요 |
| **청크 (Chunk)** | 도시락을 나눠 담기 🍱 | 큰 데이터를 작은 단위로 쪼개서 저장하는 것. 50개씩 나눠서 chunk_1.json, chunk_2.json... |
| **파싱 (Parsing)** | 편지 내용 분석 📖 | 텍스트 데이터에서 필요한 정보만 뽑아내는 작업 |
| **정규화 (Normalization)** | 모든 사람 이름을 '성+이름' 형식으로 통일 ✏️ | 비교를 위해 데이터 형태를 통일. "BBQ 치킨" → "bbq치킨" |
| **NaN (Not a Number)** | 빈칸 ⬜ | 데이터가 없음을 나타내는 특수 값. 엑셀에서 비어있는 셀과 같음 |
| **DataFrame** | 엑셀 시트 📊 | Pandas 라이브러리에서 표 형태의 데이터를 다루는 자료구조 |

### 🔧 개발 도구 / 환경 용어

| 용어 | 일상 비유 | 설명 |
|------|----------|------|
| **터미널 (Terminal)** | 컴퓨터에게 편지 쓰는 창 💻 | 마우스 클릭 대신 키보드로 명령어를 입력하는 검은 창. PowerShell, CMD, Bash 등 |
| **스크립트 (Script)** | 대본 📜 | 파이썬 명령어들을 순서대로 적어놓은 파일 (.py 확장자) |
| **pip** | 앱스토어 📲 | 파이썬 패키지(라이브러리)를 설치하는 도구. `pip install pandas` |
| **가상환경 (venv)** | 독립된 작업실 🏠 | 프로젝트마다 별도의 패키지 환경을 만들어서 충돌을 방지 |
| **환경변수 (.env)** | 금고 🔐 | API 키 같은 비밀 정보를 코드에 직접 쓰지 않고 별도 파일에 보관 |
| **백그라운드 실행** | 세탁기 돌려놓고 다른 일 하기 🧺 | 프로그램을 뒤에서 계속 돌리면서 컴퓨터로 다른 작업을 할 수 있게 하는 것 |
| **로그 (Log)** | 항해 일지 📔 | 프로그램이 실행되면서 남기는 기록. 문제가 생기면 이걸 보고 원인을 찾음 |
| **디버깅 (Debugging)** | 범인 찾기 🔍 | 코드에서 오류(버그)를 찾아서 고치는 작업 |

### 🐙 Git / GitHub 용어

| 용어 | 일상 비유 | 설명 |
|------|----------|------|
| **Git** | 타임머신 ⏰ | 코드의 변경 이력을 기록하여 언제든 과거로 돌아갈 수 있게 해주는 도구 |
| **GitHub** | 클라우드 코드 보관소 ☁️ | Git으로 관리하는 코드를 인터넷에 올려 백업하고 공유하는 서비스 |
| **Repository (저장소)** | 프로젝트 폴더 📁 | Git이 관리하는 하나의 프로젝트 단위 |
| **Commit** | 세이브 포인트 💾 | "현재 상태를 저장해!" 하는 것. 메시지와 함께 변경 내용을 기록 |
| **Push** | 업로드 ⬆️ | 내 컴퓨터의 커밋(저장)을 GitHub(인터넷)에 올리는 것 |
| **Pull** | 다운로드 ⬇️ | GitHub(인터넷)의 최신 내용을 내 컴퓨터로 가져오는 것 |
| **Branch (브랜치)** | 평행 세계 🌿 | 메인 코드를 건드리지 않고 별도의 작업 공간을 만드는 것 |
| **Merge** | 합치기 🤝 | 분리된 브랜치의 작업 내용을 메인에 합치는 것 |
| **Rebase** | 재정렬 📚 | 내 커밋들을 다른 사람의 최신 커밋 위에 다시 쌓는 것. 히스토리가 깔끔해짐 |
| **Conflict (충돌)** | 같은 문장을 두 사람이 동시에 고쳤을 때 ⚡ | 같은 부분을 서로 다르게 수정했을 때 Git이 "어느 쪽을 선택할래?"라고 물어보는 상황 |
| **.gitignore** | "이건 무시해" 목록 🙈 | Git이 추적하지 않을 파일을 지정. API 키, 로그 파일 등을 등록 |

### 🧮 수학 / 좌표 용어

| 용어 | 일상 비유 | 설명 |
|------|----------|------|
| **위도 (Latitude)** | 세로 위치 ↕️ | 지구에서 남북 방향의 위치. 적도가 0도, 북극이 90도. 부경대는 약 35.13도 |
| **경도 (Longitude)** | 가로 위치 ↔️ | 지구에서 동서 방향의 위치. 영국 그리니치가 0도. 부경대는 약 129.10도 |
| **좌표계 (Coordinate System)** | 지도의 눈금 체계 📏 | 위치를 표현하는 규칙. WGS84(GPS에서 쓰는 국제 표준), KATEC(한국에서 쓰던 옛날 방식) 등이 있음 |
| **Haversine 공식** | 지구본 위의 직선 거리 재기 📐 | 둥근 지구 표면 위의 두 점 사이 실제 거리를 계산하는 수학 공식 |
| **라디안 (Radian)** | 각도의 다른 단위 📐 | 삼각함수에서 사용하는 각도 단위. 180도 = π(파이) 라디안 |
| **해시 (Hash)** | 지문 🔏 | 어떤 데이터를 고정 길이의 고유한 문자열로 변환하는 것. 같은 입력 → 항상 같은 출력 |

### 🐍 파이썬 전용 용어

| 용어 | 일상 비유 | 설명 |
|------|----------|------|
| **pip install** | 앱 설치 📲 | 파이썬 패키지를 인터넷에서 다운받아 설치하는 명령어 |
| **import** | 도구 가져오기 🧰 | 다른 파일이나 라이브러리의 코드를 현재 코드에서 사용하겠다고 선언 |
| **try/except** | 안전벨트 🦺 | "이 코드를 실행하다 에러가 나면 프로그램을 죽이지 말고 이렇게 대처해"라는 보호 장치 |
| **f-string** | 빈칸 채우기 퀴즈 ✏️ | `f"이름은 {name}입니다"` → `"이름은 홍콩반점입니다"` 처럼 문자열 안에 변수를 끼워넣는 문법 |
| **with 문** | 자동 문 닫기 🚪 | 파일이나 연결을 열었다가 작업이 끝나면 자동으로 닫아주는 문법 |
| **lambda** | 이름 없는 일회용 함수 🏷️ | `lambda x: x * 2` → 입력의 2배를 돌려주는 간단한 함수를 한 줄로 만듦 |
| **None** | "아무것도 없음" 🕳️ | 값이 존재하지 않음을 나타내는 파이썬의 특수한 값 |
| **Set (세트)** | 중복 없는 가방 🎒 | 같은 값을 두 번 넣어도 하나만 저장되는 자료구조. 중복 체크에 완벽 |

### 📡 크롤링 전용 용어

| 용어 | 일상 비유 | 설명 |
|------|----------|------|
| **크롤러 (Crawler)** | 자동 검색 로봇 🤖 | 웹사이트나 API에서 자동으로 데이터를 수집하는 프로그램 |
| **스크래핑 (Scraping)** | 웹페이지 긁어오기 🪒 | 웹페이지의 HTML에서 원하는 정보만 추출하는 것 |
| **백오프 (Backoff)** | "잠깐 쉬었다 다시 시도" ⏸️ | 에러가 발생하면 잠시 기다렸다가 다시 시도하는 전략. 대기 시간을 점점 늘리면 "지수 백오프" |
| **화이트리스트 (Whitelist)** | 허용 목록 ✅ | "이 목록에 있는 것만 통과!" 예: 카테고리가 "음식점"인 것만 허용 |
| **필터링 (Filtering)** | 체에 거르기 🥅 | 전체 데이터에서 조건에 맞는 것만 남기고 나머지는 버리는 것 |
| **매핑 (Mapping)** | 번역/대응 🔗 | A를 B로 변환하는 규칙. 예: "음식점>한식>육류" → "고기" |
| **파이프라인 (Pipeline)** | 공장 컨베이어 벨트 🏭 | 데이터가 여러 처리 단계를 순서대로 거치는 구조 |

---

> 📅 작성일: 2026-08-12
> 🧑‍💻 프로젝트: 부경대 주변 맛집 크롤링 & 카테고리 분류
> 📝 이 문서는 공부 목적으로 작성되었으며, 실제 프로젝트 진행 과정을 그대로 기록한 것입니다.
> 🔰 비전공자도 이해할 수 있도록 모든 기술 용어에 일상 비유를 포함하였습니다.
